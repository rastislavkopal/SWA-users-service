# SWA-users-service

# How to start
Install dependencies 
`yarn`

Set env vars by copying example and fill empty values
`cp .env.example .env`

Generate docs by 
`yarn docs`

Run locally
`docker-compose up`